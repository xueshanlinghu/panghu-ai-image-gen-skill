---
name: panghu-ai-image-gen
description: "当用户需要生成图片、AI画图、文生图、图生图、多图参考改图、改图时使用，基于胖狐AI图片接口，支持 gpt-image-2.5-flare、gpt-image-2.5-sunburst 和兼容的 gpt-image-2，最高支持4K高清分辨率，面向小白用户友好交互。"
version: v1.4.1
author: 雪山凌狐
license: MIT
metadata:
  hermes:
    tags: [图片生成, AI绘图, gpt-image-2.5, gpt-image-2, 胖狐AI, 文生图, 图生图, 多图参考, 4K绘图, 小白友好]
    related_skills: [comfyui]
---

# 🦊 胖狐AI图片生成技能 - Agent操作规范

## 触发场景
当用户提出生成图片、AI画图、文生图、修改图片、图生图、多张参考图改图、改图、壁纸、头像、海报、logo、插画等图片需求时使用本技能。

## 🔴 核心交互规则（必须严格遵守）
面向普通用户时：
1. 不返回 Python/终端命令、参数名、路径配置或其他技术实现细节。
2. 用户信息不足时，用大白话询问必要内容；能自动完成的提示词适配、参数优化和错误修正全部由 Agent 完成。
3. 给出明确推荐，减少用户不必要的选择成本。
4. 不承诺后台处理，不提前中断正在进行的高清生成请求。

## 🆕 首次使用：模型 onboarding（必须执行一次）
本技能使用本地文件 `.panghu-image-models.json` 保存长期默认模型偏好。该文件不存在，或其中 `onboarding_completed` 不为 `true` 时，在第一次实际生成图片前进行一次简短 onboarding。

向用户用大白话说明：
- **gpt-image-2.5-flare**：速度更快，画质高，适合绝大多数日常文生图和快速迭代。
- **gpt-image-2.5-sunburst**：能力更强，尤其适合图生图、复杂修改、需要尽量保持原图细节和精准控制的场景，但通常更慢。
- **推荐方案**：文生图使用 Flare，图生图/改图使用 Sunburst。
- **gpt-image-2**：仍支持，作为兼容旧模型保留，需要时可单次或长期指定。

首次 onboarding 默认推荐用户选择“文生图 Flare + 图生图 Sunburst”。用户也可以选择全部使用 Flare、全部使用 Sunburst，或分别自定义两类任务的默认模型。

用户确认后，由 Agent 在技能目录执行内部配置操作，把选择写入 `.panghu-image-models.json`。此操作不得要求普通用户自己敲命令。

### 模型选择优先级
1. 用户本次明确指定模型：仅本次覆盖长期偏好。
2. 用户明确说“以后都用”“长期改成”“设为默认”等：更新 `.panghu-image-models.json` 后再生成。
3. 用户没有指定：读取 `.panghu-image-models.json`。
4. 配置文件不存在、损坏或值无效：使用内置推荐默认值：
   - 文生图：`gpt-image-2.5-flare`
   - 图生图/改图：`gpt-image-2.5-sunburst`

支持的模型值仅限：
- `gpt-image-2.5-flare`
- `gpt-image-2.5-sunburst`
- `gpt-image-2`

## 📋 用户提问流程
### 第一步：确认内容
如果用户没有说明生成什么，先询问图片内容；如果是改图且没有图片，要求用户上传需要修改的图片并说明修改要求。

如果用户提供多张图片并要求融合、参考风格、参考人物/角色/品牌元素等，先判断哪张是主图/底图、哪些是额外参考图。用户已经表达清楚时直接执行；无法可靠判断时只追问“哪张作为主图”。

### 第二步：首次使用时完成模型 onboarding
仅首次执行一次，后续直接读取已保存偏好。用户主动询问或要求调整模型时，可再次解释并修改长期偏好。

### 第三步：推荐尺寸与画质
默认优先推荐 4K + high：
- 电脑壁纸/封面：4K 横屏 3840x2160
- 手机锁屏/短视频：4K 竖屏 2160x3840
- 小红书/竖版海报：3:4 2448x3264
- 头像/朋友圈：方形 2880x2880
- 透明背景 logo/贴纸：方形 + transparent

用户没有特殊要求时直接采用推荐值，无需反复确认细枝末节。

## ⚙️ 内部调用规则（用户看不到）
调用脚本：`python scripts/generate.py [参数]`，工作目录为技能根目录。

> 接口参数、请求格式和错误处理见 [references/api-notes.md](./references/api-notes.md)。
> 提示词模板见 [references/prompt-templates.md](./references/prompt-templates.md)。

### 接口调用强制规则
1. 文生图（无上传图片）：`/images/generations`，JSON 请求。
2. 图生图/改图（有上传图片）：`/images/edits`，multipart/form-data 上传。
3. 单图改图时，主图通过 `--image` 传入，对应 multipart 字段 `image`。
4. 多图参考时，主图仍通过 `--image` 传入；每张额外参考图各使用一个 `--image-extra`，对应重复的 multipart 字段 `image[]`。禁止把多张图片都塞进同一个 `image` 字段。
5. `--image-extra` 不能脱离 `--image` 单独使用。
6. 输入图片 MIME：jpg/jpeg→`image/jpeg`、png→`image/png`、webp→`image/webp`。
7. 模型由脚本按“单次覆盖 > 本地偏好 > 内置推荐”自动解析。

### 多图参考使用策略
- 主图/底图决定整体基础内容；额外参考图用于补充风格、人物/角色形象、logo/品牌元素或其他需要融合的视觉参考。
- 只有用户确实需要融合多张参考时才使用额外参考图。若目标是局部修改且其余内容尽量保持不变，优先单图改图，并在提示词中明确唯一调整项及不可改动内容。
- 多图参考更容易触发整图重绘、文字或局部元素走样。生成后优先依据用户对原图与结果的判断，不要因 Agent 自身视觉误判连续重生成浪费积分。
- 若额外参考图为 JPG/JPEG 且接口只返回无具体原因的 `400 bad_request`，可在具备图片转换能力时先将疑似有问题的参考图转为 PNG 后重试一次；不要为此强制安装额外依赖，也不要连续盲目重试。

### 内部模型配置操作
以下仅供 Agent 内部执行，普通用户不可见：
- 单次指定：生成时使用 `--model <模型>`，不写入长期配置。
- 长期修改文生图默认：`--set-text-model <模型>`。
- 长期修改图生图默认：`--set-edit-model <模型>`。
- 可同时修改两个长期默认；脚本会写入 `.panghu-image-models.json` 并标记 onboarding 完成。
- 查看当前偏好：`--show-model-config`。

### 默认生成参数
用户没说时使用 `--quality high`、`--size "3840x2160"`、`--output_format png`，自动保存到 `./生成结果/`。透明背景自动改用 png，避免透明通道失效。

### 超时与重试
1. 后台生成请求超时为 30 分钟，禁止提前中断或因等待较久盲目重复提交，避免重复扣费。
2. 用户侧统一说明“高清/4K 图片生成可能需要 1-5 分钟，请耐心等待不要中断”。
3. 下载图片超时 5 分钟。
4. 网络请求失败可重试，但必须确认前一次请求已经明确失败；最多重试 2 次。

## ✅ 成功后
如果消息平台支持图片发送，直接发送生成图片，并告知耗时和保存位置。不要暴露接口、参数、脚本等内部实现。

## ❌ 常见错误处理
- 提示词过长：自动精简后重试。
- 尺寸不支持：自动换成最接近的推荐尺寸。
- 密钥/额度问题：告知当前账号额度或配置存在问题。
- 网络错误：确认请求已明确失败后再重试，最多 2 次。
